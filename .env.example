# Variáveis de Ambiente para o Projeto

# --- Configurações Gerais ---
# ENVIRONMENT: Define o ambiente (e.g., development, staging, production)
ENVIRONMENT=development
# DEBUG: Habilita o modo de debug (True/False)
DEBUG=True
# ENV: Alias para o ambiente (e.g., local, docker, k8s)
ENV=local

# --- Configurações de Segurança ---
# SECRET_KEY: Chave secreta para JWT e criptografia. 
SECRET_KEY="SUA_CHAVE_SECRETA_AQUI"
# ALGORITHM: Algoritmo de criptografia (e.g., HS256)
ALGORITHM=HS256
# ACCESS_TOKEN_EXPIRE_MINUTES: Tempo de expiração do token de acesso em minutos
ACCESS_TOKEN_EXPIRE_MINUTES=30

# --- Configurações de Banco de Dados (PostgreSQL) ---
# DATABASE_URL: String de conexão com o banco de dados (formato: postgresql://user:password@host:port/dbname)
DATABASE_URL="postgresql://santander:santander123@postgres:5432/credit_risk"

# --- Configurações de Cache (Redis) ---
# REDIS_URL: String de conexão com o Redis (formato: redis://:password@host:port/db)
REDIS_URL="redis://:redis123@redis:6379/0"

# --- Configurações do Modelo ML ---
# MODEL_PATH: Caminho para o arquivo do modelo (relativo à raiz do projeto)
# Exemplo: MODEL_PATH=api/modelo_lgbm.pkl
# MODEL_VERSION: Versão do modelo
MODEL_VERSION=1.0.0
# THRESHOLD_DEFAULT: Limiar de classificação padrão
THRESHOLD_DEFAULT=0.50

# --- Configurações de Logging ---
LOG_LEVEL=INFO

# --- Configurações de Limite de Requisições ---
# RATE_LIMIT_ENABLED: Habilita o limite de requisições (True/False)
RATE_LIMIT_ENABLED=False
# RATE_LIMIT_PER_MINUTE: Número máximo de requisições por minuto
RATE_LIMIT_PER_MINUTE=100

# --- Configurações de Monitoramento e Features ---
# METRICS_ENABLED: Habilita a exposição de métricas (True/False)
METRICS_ENABLED=True
# FEATURE_EXPLAINABILITY: Habilita a explicabilidade do modelo (True/False)
FEATURE_EXPLAINABILITY=True
# FEATURE_MONITORING: Habilita o monitoramento de desvio de dados (True/False)
FEATURE_MONITORING=True

# --- Caminho para Dados ---
# DATA_PATH: Caminho para a pasta de dados processados.
# Exemplo: DATA_PATH=${PROJECT_ROOT}/data/processed
# DATA_PATH_OVERRIDE: Usado para sobrescrever o DATA_PATH em ambientes específicos
# DATA_PATH_OVERRIDE=
